# Aves del Sur — production domain and email wiring report

24 September 2026. **Code is prepared for `https://avesdelsur.com/`, but this Replit project is not published and the domain was not reachable in the external checks performed here.** No DNS records, TLS certificates, mailbox or email-delivery service were changed.

## Configuration completed

1. **Domain:** `SITE_URL` is fixed to `https://avesdelsur.com` without a trailing slash in `tools/site/config.mjs`; `BASE_URL` did not exist and was not added. The site name remains Aves del Sur. Public domain/email are not secrets, so build-time constants keep all generated pages on one origin; no environment variables were added or changed.
2. **Email:** `BRAND.email` is `info@avesdelsur.com`. Footer, contact, legal notice and privacy links use `mailto:info@avesdelsur.com`. Shared Organization JSON-LD includes the same email. Mailbox reception is unverified.
3. **SEO:** 48 generated HTML pages have HTTPS absolute canonical, `og:url`, `og:image`, `twitter:image` and relevant JSON-LD URLs/IDs, including BreadcrumbList and Article links. Favicon, page navigation, CSS and JS use relative same-origin paths, which remain valid. No `http://avesdelsur.com`, www, localhost or preview-domain page references were found. No unsupported LocalBusiness/Product properties were created.
4. **Sitemap:** `site/sitemap.xml` has 47 canonical HTTPS URLs, excludes noindex `/404/`. `/sitemap.xml` now returns those generated bytes rather than using the incoming request origin, which previously produced HTTP URLs behind the HTTPS development proxy. The source generator remains `tools/build-site.mjs`; regenerate it when content changes.
5. **Robots:** both `site/robots.txt` and served `public/robots.txt` allow crawling and reference `https://avesdelsur.com/sitemap.xml`. No admin/private paths exist in the generated public route inventory, so no speculative Disallow entries were added.
6. **Contact form:** still has no submission endpoint/provider. The form explicitly says so and provides the direct email link; its failure message no longer suggests an unverified WhatsApp number. It does not report false success. [EMAIL-SETUP.md](EMAIL-SETUP.md) contains the configuration and test plan.

## Files changed

- Source: `tools/site/config.mjs`, `tools/site/layout.mjs`, `tools/site/pages.mjs`, `tools/site/components.mjs`, `src/routes/sitemap[.]xml.ts`, `public/robots.txt`, `public/assets/js/site.js`.
- Generated: `site/**/index.html`, `site/sitemap.xml`, `site/robots.txt`; refreshed seven reports in `seo-audit/`.
- Documentation: `BUSINESS-IDENTITY-AUDIT.md`, `EMAIL-SETUP.md`, this report, and a historical-status note in `SEO-ACTION-PLAN.md`.
- No `.env.example` was needed: no new runtime settings or credentials are consumed. Future mail-provider variables are documented, not falsely presented as active configuration.

## Verification

- `bun tools/build-site.mjs`: **pass**, 48 HTML pages.
- `bun run seo:audit`: **pass**, 47 indexable and one noindex route; 47 absolute HTTPS sitemap entries; zero missing page titles, descriptions or H1s, zero broken generated page references and zero invalid JSON-LD. Remaining audit groups cover the unconfigured form, other business placeholders and the pre-existing large logo.
- `bunx tsc --noEmit`: **pass**.
- `bun run build`: **pass** (TanStack/Nitro production bundle).
- Focused ESLint on changed sitemap route and components: **pass**. A broader lint check surfaced pre-existing Prettier formatting errors in source files; full `eslint .` also traversed a large cache and timed out. Neither was modified outside this wiring scope.
- Development preview GET checks are separate from production checks. Recheck `/`, `/contacto/`, `/sitemap.xml`, `/robots.txt` and unknown-page status after restart; the actual public domain is currently untestable. No real email submission was attempted.

## Manual publishing, HTTPS, redirects and email work

The deployment service reports **no active published deployment**. Direct requests to `https://avesdelsur.com/`, its HTTP variant and the corresponding www hosts could not resolve here. Public DNS lookup at audit time found no apex A answer and `www` returned NXDOMAIN; this is a point-in-time observation, not a claim about future configuration. Therefore HTTPS availability, certificate, HTTP→HTTPS behavior, www→apex behavior, live robots/sitemap and mailbox delivery are **unverified**.

1. Publish this Replit project. In Publishing → Domains, connect `avesdelsur.com` and copy the exact A and TXT records shown by Replit to the domain's registrar/DNS host; do not guess IPs or verification tokens. Replit's [custom-domain documentation](https://docs.replit.com/build/add-custom-domain) says HTTPS certificates are provisioned after verification. Verify `https://avesdelsur.com/` loads the newly published build.
2. Only if `www.avesdelsur.com` is wanted, add it separately in domain settings and configure an explicit permanent www→apex redirect with the host/DNS provider. Test both hostnames and both schemes for one-way redirects; do not add a request-origin redirect inside the app before the proxy/host setup is known, since that could loop.
3. Configure the business mailbox and its MX records with the mail provider; verify a real external email arrives. Sender-domain SPF/DKIM/DMARC records come from the selected mail provider, **not** the website's A/TXT records. Implement and test a server-side email provider before enabling automatic contact-form delivery.
4. The business has since supplied Granadilla de Abona, Tenerife as its locality and confirmed shipping coverage throughout Spain and Europe; these now appear in the site and Organization JSON-LD. A complete legal street address remains unprovided. Replace the remaining legal and phone placeholders only with confirmed details. Opening hours have not been provided and are not published.

After publishing, re-run an external crawl of HTTPS apex, HTTP apex, HTTP/HTTPS www (if configured), sitemap, robots and a missing page. Confirm the TLS certificate, canonical/OG/JSON-LD host consistency, redirects and absence of mixed content on the actual deployed site; update this report with those **live** outcomes rather than assuming the development preview proves them.